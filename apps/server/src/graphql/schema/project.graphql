type Project implements Node {
  id: ID!
  name: String!
  description: String!
  client: Client!
  user: User!
  status: Status!
}

type ProjectEdge {
  node: Project!
  cursor: String!
}

type ProjectConnection {
  edges: [ProjectEdge]!
  pageInfo: PageInfo!
}

input ProjectInput {
  name: String!
  description: String
  client: ID!
  user: ID!
  status: Status!
}

input UpdateProjectInput {
  name: String
  description: String
  status: Status
  client: ID
  user: ID
}

input ProjectFilter {
  name: String
  status: Status
  client: ID
  user: ID
}

type Query {
  projects(first: Int, after: String, last: Int, before: String, filter: ProjectFilter): ProjectConnection!
  project(id: ID!): Project!
  projectCount(user: ID): Int!
}

type Mutation {
  createProject(input: ProjectInput!): Project!
  updateProject(id: ID!, input: UpdateProjectInput!): Project!
  deleteProject(id: ID!): String!
}

type Subscription {
  projectCount: Int!
}
